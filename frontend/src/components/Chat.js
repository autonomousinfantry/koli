import React from 'react';

const Chat = ({ projectId }) => {
    return (
        <div className="chat">
            <h2>Chat</h2>
            {/* Chat implementation */}
        </div>
    );
};

export default Chat;
